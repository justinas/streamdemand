[tool.poetry]
name = "streamdemand"
version = "0.1.0"
description = ""
authors = ["Justinas Stankevicius <justinas@justinas.org>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
Flask = "^2.0"
streamlink = { git = "https://github.com/justinas/streamlink", branch = "add-plugin-lnk" }
gunicorn = "^20.1.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
